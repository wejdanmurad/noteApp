<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.noteapp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ViewMyNote"></activity>
        <activity android:name=".ShowAllNotes" />
        <activity android:name=".ViewBooks" />
        <activity android:name=".AddBook" />
        <activity android:name=".ViewNotes" />
        <activity android:name=".signInUp.Splash" />
        <activity android:name=".signInUp.SignUp" />
        <activity android:name=".signInUp.SignIn" />
        <activity android:name=".signInUp.Tutorial">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AddNote" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>